meta {
  name: 获取执行详情
  type: http
  seq: 2
}

get {
  url: {{ADMIN_URL}}/executions/{{EXECUTION_ID}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{ADMIN_TOKEN}}
}

tests {
  test("Status code is 200", function() {
    expect(res.status).to.equal(200);
  });
}
