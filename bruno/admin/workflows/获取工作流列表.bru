meta {
  name: 获取工作流列表
  type: http
  seq: 1
}

get {
  url: {{ADMIN_URL}}/workflows?page=1&pageSize=20&status=active&userId=&search=test&sortBy=createdAt&order=desc
  body: none
  auth: bearer
}

auth:bearer {
  token: {{ADMIN_TOKEN}}
}

tests {
  test("Status code is 200", function() {
    expect(res.status).to.equal(200);
  });
}
