meta {
  name: 获取系统设置
  type: http
  seq: 1
}

get {
  url: {{ADMIN_URL}}/settings
  body: none
  auth: bearer
}

auth:bearer {
  token: {{ADMIN_TOKEN}}
}

tests {
  test("Status code is 200", function() {
    expect(res.status).to.equal(200);
  });
}
