meta {
  name: 获取系统日志-无效Token
  type: http
  seq: 2
}

get {
  url: {{ADMIN_URL}}/logs
  body: none
  auth: bearer
}

auth:bearer {
  token: invalid_token_12345
}

tests {
  test("Status code is 401", function() {
    expect(res.status).to.equal(401);
  });
}
