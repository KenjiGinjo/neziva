meta {
  name: 获取系统日志-正常
  type: http
  seq: 3
}

get {
  url: {{ADMIN_URL}}/admin/logs
  body: none
  auth: bearer
}

params:query {
  page: 1
  limit: 20
  level:
  startDate:
  endDate:
  search:
}

tests {
  test("Status code is 200", function() {
    expect(res.status).to.equal(200);
  });
  
  test("Response contains logs data", function() {
    expect(res.body).to.have.property('data');
    expect(res.body).to.have.property('pagi');
    expect(res.body.data).to.be.an('array');
    expect(res.body.pagi).to.have.property('total');
  });
}
