meta {
  name: 获取联系表单列表-搜索关键词
  type: http
  seq: 7
}

get {
  url: {{ADMIN_URL}}/admin/contact/forms
  body: none
  auth: bearer
}

params:query {
  page: 1
  limit: 20
  status:
  projectType:
  search: John
}

tests {
  test("Status code is 200", function() {
    expect(res.status).to.equal(200);
  });
}
