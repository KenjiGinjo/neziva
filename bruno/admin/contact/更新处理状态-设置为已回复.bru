meta {
  name: 更新处理状态-设置为已回复
  type: http
  seq: 4
}

put {
  url: {{ADMIN_URL}}/admin/contact/forms/:id/status
  body: json
  auth: bearer
}

params:path {
  id: clx1234567890
}

body:json {
  {
    "status": 2
  }
}

tests {
  test("Status code is 204", function() {
    expect(res.status).to.equal(204);
  });
}
