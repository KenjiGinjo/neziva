meta {
  name: 获取联系表单列表-无Token
  type: http
  seq: 2
}

get {
  url: {{ADMIN_URL}}/admin/contact/forms
  body: none
  auth: none
}

tests {
  test("Status code is 401", function() {
    expect(res.status).to.equal(401);
  });
}
