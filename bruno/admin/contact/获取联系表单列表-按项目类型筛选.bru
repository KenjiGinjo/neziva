meta {
  name: 获取联系表单列表-按项目类型筛选
  type: http
  seq: 6
}

get {
  url: {{ADMIN_URL}}/admin/contact/forms
  body: none
  auth: bearer
}

params:query {
  page: 1
  limit: 20
  status:
  projectType: strategy
  search:
}

tests {
  test("Status code is 200", function() {
    expect(res.status).to.equal(200);
  });
}
