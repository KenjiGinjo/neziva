meta {
  name: 获取用户统计
  type: http
  seq: 5
}

get {
  url: {{ADMIN_URL}}/users/stats?startDate=2024-01-01&endDate=2024-12-31
  body: none
  auth: bearer
}

auth:bearer {
  token: {{ADMIN_TOKEN}}
}

tests {
  test("Status code is 200", function() {
    expect(res.status).to.equal(200);
  });
}
