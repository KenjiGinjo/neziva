meta {
  name: 删除用户
  type: http
  seq: 4
}

delete {
  url: {{ADMIN_URL}}/users/{{USER_ID}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{ADMIN_TOKEN}}
}

tests {
  test("Status code is 200", function() {
    expect(res.status).to.equal(200);
  });
}
