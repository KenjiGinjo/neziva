meta {
  name: 获取用户列表
  type: http
  seq: 1
}

get {
  url: {{ADMIN_URL}}/users?page=1&pageSize=20&status=active&subscriptionPlan=pro&search=test&sortBy=createdAt&order=desc
  body: none
  auth: bearer
}

auth:bearer {
  token: {{ADMIN_TOKEN}}
}

tests {
  test("Status code is 200", function() {
    expect(res.status).to.equal(200);
  });
}
