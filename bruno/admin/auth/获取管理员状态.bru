meta {
  name: 获取管理员状态
  type: http
  seq: 2
}

get {
  url: {{ADMIN_URL}}/auth/state
  body: none
  auth: bearer
}

auth:bearer {
  token: {{ADMIN_TOKEN}}
}

tests {
  test("Status code is 200", function() {
    expect(res.status).to.equal(200);
  });
}
