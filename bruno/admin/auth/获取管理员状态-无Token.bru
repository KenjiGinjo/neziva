meta {
  name: 获取管理员状态-无Token
  type: http
  seq: 2
}

get {
  url: {{ADMIN_URL}}/auth/state
  body: none
  auth: none
}

tests {
  test("Status code is 401", function() {
    expect(res.status).to.equal(401);
  });
}
