meta {
  name: 获取管理员状态-无效Token
  type: http
  seq: 3
}

get {
  url: {{ADMIN_URL}}/auth/state
  body: none
  auth: bearer
}

auth:bearer {
  token: invalid_token_12345
}

tests {
  test("Status code is 401", function() {
    expect(res.status).to.equal(401);
  });
}
