meta {
  name: 获取博客文章列表-按标签筛选
  type: http
  seq: 8
}

get {
  url: {{ADMIN_URL}}/admin/blog/posts
  body: none
  auth: bearer
}

params:query {
  page: 1
  pageSize: 20
  status:
  category:
  tag: AI Development
  search:
}

tests {
  test("Status code is 200", function() {
    expect(res.status).to.equal(200);
  });
}
