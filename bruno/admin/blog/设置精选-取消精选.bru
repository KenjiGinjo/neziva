meta {
  name: 设置精选-取消精选
  type: http
  seq: 4
}

put {
  url: {{ADMIN_URL}}/admin/blog/posts/:id/feature
  body: json
  auth: bearer
}

params:path {
  id: clx1234567890
}

body:json {
  {
    "featured": false
  }
}

tests {
  test("Status code is 204", function() {
    expect(res.status).to.equal(204);
  });
}
