meta {
  name: 设置精选-文章不存在
  type: http
  seq: 2
}

put {
  url: {{ADMIN_URL}}/api/admin/blog/posts/non-existent-id-12345/feature
  body: json
  auth: bearer
}

body:json {
  {
    "featured": true
  }
}

tests {
  test("Status code is 404", function() {
    expect(res.status).to.equal(404);
  });
}
