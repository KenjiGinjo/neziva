meta {
  name: 设置精选-设置为精选
  type: http
  seq: 3
}

put {
  url: {{ADMIN_URL}}/api/admin/blog/posts/:id/feature
  body: json
  auth: bearer
}

params:path {
  id: clx1234567890
}

body:json {
  {
    "featured": true
  }
}

tests {
  test("Status code is 204", function() {
    expect(res.status).to.equal(204);
  });
}
