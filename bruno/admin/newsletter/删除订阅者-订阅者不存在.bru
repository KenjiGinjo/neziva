meta {
  name: 删除订阅者-订阅者不存在
  type: http
  seq: 2
}

delete {
  url: {{ADMIN_URL}}/api/admin/newsletter/subscribers/non-existent-id-12345
  body: none
  auth: bearer
}

tests {
  test("Status code is 404", function() {
    expect(res.status).to.equal(404);
  });
}
