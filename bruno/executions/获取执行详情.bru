meta {
  name: 获取执行详情
  type: http
  seq: 1
}

get {
  url: {{API_URL}}/executions/{{EXECUTION_ID}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{USER_TOKEN}}
}

tests {
  test("Status code is 200", function() {
    expect(res.status).to.equal(200);
  });
}
