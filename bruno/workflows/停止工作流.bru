meta {
  name: 停止工作流
  type: http
  seq: 7
}

post {
  url: {{API_URL}}/workflows/{{WORKFLOW_ID}}/stop
  body: none
  auth: bearer
}

auth:bearer {
  token: {{USER_TOKEN}}
}

tests {
  test("Status code is 200", function() {
    expect(res.status).to.equal(200);
  });
}
