meta {
  name: 删除工作流
  type: http
  seq: 5
}

delete {
  url: {{API_URL}}/workflows/{{WORKFLOW_ID}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{USER_TOKEN}}
}

tests {
  test("Status code is 200", function() {
    expect(res.status).to.equal(200);
  });
}
