meta {
  name: 获取工作流执行历史
  type: http
  seq: 9
}

get {
  url: {{API_URL}}/workflows/{{WORKFLOW_ID}}/executions?page=1&pageSize=20&status=completed
  body: none
  auth: bearer
}

auth:bearer {
  token: {{USER_TOKEN}}
}

tests {
  test("Status code is 200", function() {
    expect(res.status).to.equal(200);
  });
}
