meta {
  name: 获取工作流详情
  type: http
  seq: 3
}

get {
  url: {{API_URL}}/workflows/{{WORKFLOW_ID}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{USER_TOKEN}}
}

tests {
  test("Status code is 200", function() {
    expect(res.status).to.equal(200);
  });
}
