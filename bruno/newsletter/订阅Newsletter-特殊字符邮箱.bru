meta {
  name: 订阅Newsletter-特殊字符邮箱
  type: http
  seq: 7
}

post {
  url: {{API_URL}}/newsletter/subscribe
  body: json
  auth: none
}

body:json {
  {
    "email": "user+test@example.com",
    "source": "homepage"
  }
}

tests {
  test("Status code is 200", function() {
    expect(res.status).to.equal(200);
  });
}
