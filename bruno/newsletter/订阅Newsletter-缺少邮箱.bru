meta {
  name: 订阅Newsletter-缺少邮箱
  type: http
  seq: 3
}

post {
  url: {{API_URL}}/newsletter/subscribe
  body: json
  auth: none
}

body:json {
  {
    "source": "homepage"
  }
}

tests {
  test("Status code is 400", function() {
    expect(res.status).to.equal(400);
  });
}
