meta {
  name: 订阅Newsletter-空邮箱
  type: http
  seq: 4
}

post {
  url: {{API_URL}}/newsletter/subscribe
  body: json
  auth: none
}

body:json {
  {
    "email": "",
    "source": "homepage"
  }
}

tests {
  test("Status code is 400", function() {
    expect(res.status).to.equal(400);
  });
}
