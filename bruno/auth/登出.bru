meta {
  name: 登出
  type: http
  seq: 3
}

post {
  url: {{API_URL}}/auth/logout
  body: none
  auth: bearer
}

auth:bearer {
  token: {{USER_TOKEN}}
}

tests {
  test("Status code is 200", function() {
    expect(res.status).to.equal(200);
  });
}
