meta {
  name: 获取账单历史
  type: http
  seq: 5
}

get {
  url: {{API_URL}}/billing/invoices?page=1&pageSize=20
  body: none
  auth: bearer
}

auth:bearer {
  token: {{USER_TOKEN}}
}

tests {
  test("Status code is 200", function() {
    expect(res.status).to.equal(200);
  });
}
