meta {
  name: 搜索博客文章-长关键词
  type: http
  seq: 4
}

get {
  url: {{API_URL}}/api/blog/search
  body: none
  auth: none
}

params:query {
  page: 1
  limit: 10
  keyword: This is a very long search keyword that might test the system's ability to handle long search queries
}

tests {
  test("Status code is 200", function() {
    expect(res.status).to.equal(200);
  });
}
